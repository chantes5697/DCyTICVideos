@model IEnumerable<DCyTICVideos.Formato>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.formato1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.formato1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_formato }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_formato }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_formato })
        </td>
    </tr>
}

</table>
